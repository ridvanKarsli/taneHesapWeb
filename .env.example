# Backend API'nin base URL'i (TaneHesap.API). Yerelde varsayılan: http://localhost:5000
# Bu dosyayı .env olarak kopyalayıp kendi ortamınıza göre düzenleyin; .env git'e commit edilmez.
VITE_API_BASE_URL=http://localhost:5000
